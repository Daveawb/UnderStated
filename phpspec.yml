suites:
    main:
        namespace: UnderStated\Examples
        psr4_prefix: UnderStated\Examples
        src_path : examples
        spec_path : tests/examples
    src:
        namespace: UnderStated
        psr4_prefix: UnderStated
        src_path : src
        spec_path : tests/fsm

extensions:
    - PhpSpec\NyanFormattersExtension\Extension
formatter.name: nyan.cat